[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — React Router handles all routes client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
